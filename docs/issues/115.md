# Issue #115: Configure cron job to clean up stale party session data

**GitHub:** https://github.com/vitorsilva/saberloop/issues/115
**Status:** Plan Approved
**Created:** 2026-01-15
**Labels:** enhancement, party-mode
**Branch:** `chore/issue-115-party-cleanup-cron`

---

## Problem

The Party Mode backend (PHP + MySQL) stores session data for room management and WebRTC signaling. A cleanup script already exists (`php-api/party/cleanup.php`) but **no cron job is configured on the VPS** to run it automatically, which means stale/abandoned party sessions accumulate in the database over time.

## Proposed Solution

Configure a cron job on the VPS to periodically clean up old party session data.

### Data to Clean
- Expired/abandoned rooms
- Old signaling messages
- Disconnected peer records
- Any other temporary party session data

### Considerations
- Retention periods already configured in `php-api/party/config.php`:
  - Rooms: 2 hours (`room.expiry_hours`)
  - Signaling messages: 60 seconds (`signaling.message_expiry_seconds`)
- Script suggests running every 15 minutes (see comment in `cleanup.php`)
- Cleanup script already exists and logs operations to stdout

## Acceptance Criteria

- [x] Cleanup script/endpoint exists in PHP backend (`php-api/party/cleanup.php`)
- [ ] Cron job configured on VPS
- [ ] Stale data is automatically removed on schedule (verify after cron setup)
- [x] Cleanup is logged for monitoring (script outputs to stdout, redirect to log file)
- [x] Active sessions are not affected (only rooms with `status='active'` older than expiry are ended)
- [x] Documentation updated with cron configuration (see [DEPLOYMENT.md](../architecture/DEPLOYMENT.md#cron-jobs-via-cpanel))

---

## Solution Design

### Existing Cleanup Script

The cleanup script already exists at `php-api/party/cleanup.php` and:
1. Ends rooms older than `expiry_hours` (default 2 hours)
2. Deletes signaling messages older than `message_expiry_seconds` (default 60 seconds)
3. Deletes old rate limit records
4. Logs cleanup operations to stdout

### Current Retention Policy (in `config.php`)

| Data Type | Retention | Config Key |
|-----------|-----------|------------|
| Rooms | 2 hours | `room.expiry_hours` |
| Signaling messages | 60 seconds | `signaling.message_expiry_seconds` |

### Cron Schedule (recommended)

Run every 15 minutes:

**Via cPanel (Cron Jobs):**
1. Log in to cPanel
2. Navigate to **Cron Jobs** (under "Advanced" section)
3. Set schedule: `*/15 * * * *` (every 15 minutes)
   - Or use the dropdowns: "Every 15 minutes", "Every hour", "Every day", "Every month", "Every weekday"
4. Set command:
   ```
   /usr/bin/php /home/USERNAME/public_html/party/cleanup.php >> /home/USERNAME/public_html/party/logs/cleanup.log 2>&1
   ```
   - Replace `USERNAME` with your cPanel username
   - Party backend is deployed to `/party/` (not `/app/php-api/party/`)
5. Click **Add New Cron Job**

**Notes:**
- Create the `logs` directory first if it doesn't exist
- Use full paths (cPanel cron doesn't have the same PATH as interactive shell)
- Find PHP path with cPanel's "Terminal" or ask hosting support (usually `/usr/bin/php` or `/usr/local/bin/php`)

---

## Files to Change

| File | Change |
|------|--------|
| `php-api/party/cleanup.php` | Already exists - no changes needed |
| `php-api/party/RoomManager.php` | Already has `cleanup()` method - no changes needed |
| `php-api/party/SignalingManager.php` | Already has `cleanup()` method - no changes needed |
| `docs/architecture/DEPLOYMENT.md` | ✅ Updated with cron setup via cPanel |
| cPanel Cron Jobs | Configure cron job (manual step via cPanel UI) |

---

## Testing Plan

### Manual Testing (Required)

**Justification:** This is a backend operational task that requires VPS access. Automated testing is impractical due to:
- Requires MySQL database with test data
- Requires cron execution environment
- Involves time-based operations

### Manual Testing Checklist

- [ ] Cleanup script runs without errors
- [ ] Old rooms are deleted (create test room, wait, verify deletion)
- [ ] Active rooms are NOT deleted
- [ ] Old signaling messages are deleted
- [ ] Cleanup is logged to file/stdout
- [ ] Cron job executes on schedule
- [ ] No impact on active party sessions

### Verification Steps

**Via cPanel:**
1. **Check cron is configured:**
   - Go to cPanel → Cron Jobs
   - Verify the cleanup job appears in "Current Cron Jobs" list

2. **Run manually to verify (via cPanel Terminal or File Manager):**
   - Option A: Use cPanel Terminal (if available):
     ```bash
     php ~/public_html/party/cleanup.php
     ```
   - Option B: Access via browser with token:
     ```
     https://saberloop.com/party/cleanup.php?token=YOUR_CLEANUP_TOKEN
     ```
     (Set `CLEANUP_TOKEN` env var on server first)

3. **Check logs:**
   - Go to cPanel → File Manager
   - Navigate to `public_html/party/logs/`
   - View `cleanup.log` file

---

## Pre-Implementation Checklist

- [x] Root cause identified (N/A - operational task, script exists but cron not configured)
- [x] Solution designed (script already implemented)
- [x] Testing plan defined
- [x] Branch created (not needed - documentation only)
- [x] Plan validated by user
- [ ] cPanel access confirmed

---

## Implementation Notes

*To be filled during implementation.*

---

## Branch & Commit Strategy

### Branch

```
chore/issue-115-party-cleanup-cron
```

Created from `main` in worktree: `../demo-pwa-app-issue-115`

### Commit Plan

| Order | Commit Message | Status |
|-------|----------------|--------|
| 1 | `docs(architecture): expand deployment guide for all components` | ✅ Done (e9e2dcb) |
| 2 | `docs(issues): update plan for issue 115` | This commit |

**Note:** Cleanup script already exists - only cron configuration needed on VPS.

### Commit Message Format

```
<type>(scope): <description>

- Detail 1
- Detail 2

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
```

---

## References

- PHP Backend: `php-api/party/` (deployed to `/party/`)
- Database: MySQL (party rooms, signaling)
- [Deployment Guide - Cron Jobs](../architecture/DEPLOYMENT.md#cron-jobs-via-cpanel)
- [System Overview - PHP Backend](../architecture/SYSTEM_OVERVIEW.md#php-backend-php-api)
- [Party Mode Phase 3](../learning/epic06_sharing/PHASE3_PARTY_SESSION.md)
