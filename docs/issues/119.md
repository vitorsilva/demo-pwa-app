# Issue #119: Fix TypeScript/JSDoc type annotation errors in CI

**GitHub:** https://github.com/vitorsilva/saberloop/issues/119
**Status:** Open
**Created:** 2026-01-15
**Labels:** bug, technical-debt, epic-10-hygiene
**Branch:** `fix/issue-119-typescript-errors`

---

## Problem

GitHub Actions CI is reporting TypeScript type annotation errors on several files. These are pre-existing issues that need to be resolved.

---

## Affected Files

### src/main.js (line 252)
```
Property 'userAgent' does not exist on type 'never'.
```

### src/core/router.js (line 244)
```
This expression is not constructable.
```

### src/components/ModeToggle.js (line 56)
```
Property 'dataset' does not exist on type 'Element'.
```

### src/api/openrouter-client.js (lines 51, 52, 62, 71, 176)
```
Property 'status' does not exist on type 'Error'.
Property 'response' does not exist on type 'Error'.
```

---

## Root Cause

JSDoc type annotations are incomplete or incorrect, causing TypeScript strict mode checks to fail.

---

## Suggested Fixes

1. **main.js**: Add proper type narrowing for `navigator`
2. **router.js**: Fix ViewClass type definition to be constructable
3. **ModeToggle.js**: Cast `btn` to `HTMLElement` instead of `Element`
4. **openrouter-client.js**: Create a custom error type that includes `status` and `response` properties

---

## Acceptance Criteria

- [ ] All TypeScript/JSDoc errors in CI are resolved
- [ ] No new type errors introduced
- [ ] CI pipeline passes cleanly

---

## Files to Change

| File | Change |
|------|--------|
| `src/main.js` | Add type narrowing for navigator |
| `src/core/router.js` | Fix ViewClass type to be constructable |
| `src/components/ModeToggle.js` | Cast to HTMLElement |
| `src/api/openrouter-client.js` | Create/use custom error type |

---

## Branch & Commit Strategy

### Branch

```
fix/issue-119-typescript-errors
```

Created from `main` in worktree: `../demo-pwa-app-issue-119`

### Commit Plan

| Order | Commit Message | Description |
|-------|----------------|-------------|
| 1 | `fix(types): add proper type narrowing to main.js` | Fix navigator.userAgent error |
| 2 | `fix(types): fix ViewClass type definition in router.js` | Make type constructable |
| 3 | `fix(types): cast btn to HTMLElement in ModeToggle.js` | Fix dataset property error |
| 4 | `fix(types): create custom error type for openrouter-client.js` | Fix status/response errors |
| 5 | `docs(issues): update plan for issue 119` | Update this document |

### Commit Message Format

```
<type>(scope): <description>

- Detail 1
- Detail 2

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
```

---

## References

- [Epic 10 Hygiene Plan](../learning/epic10_hygiene/EPIC10_HYGIENE_PLAN.md)
