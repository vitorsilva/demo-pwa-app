# Issue #143: Shared quiz links redirect new users to onboarding instead of quiz

**GitHub:** https://github.com/vitorsilva/saberloop/issues/143
**Status:** Open
**Created:** 2026-01-19
**Labels:** bug
**Branch:** `fix/issue-143-shared-quiz-onboarding`

---

## Problem

When a new user clicks a shared quiz link (e.g., `https://saberloop.com/app/#/quiz/play/QUIZID`) and it's their first time accessing the app, they get redirected to the onboarding/welcome screen instead of going directly to the quiz.

**User Impact:**
- Confusing experience - user clicked a specific quiz link but landed somewhere unexpected
- Delays engagement - user must complete onboarding before playing the quiz
- Social friction - user received a quiz from a friend but can't immediately play it
- Potential drop-off - new users might give up before completing onboarding

**Expected Behavior:**
New users with a shared quiz link should go directly to the quiz, bypassing the onboarding flow.

---

## Root Cause Analysis

Same timing/order problem as issue #128 in `src/main.js`:

1. **Router initializes first**: Processes `#/quiz/play/QUIZID` and renders QuizView
2. **Onboarding check runs after**: Checks if user needs onboarding and redirects to `#/welcome`
3. **Result**: The welcome redirect overwrites the quiz view

```javascript
// src/main.js - current behavior
const showWelcome = await shouldShowWelcome();
if (showWelcome && !isPartyJoinRoute) {  // Only checks party join
  window.location.hash = '#/welcome';  // Overwrites shared quiz URL
}
```

---

## Proposed Solution

**Extend the party join fix to also skip onboarding for shared quiz URLs.**

### Implementation

Modify `src/main.js` to also check for shared quiz routes:

```javascript
// Check if current URL should bypass onboarding
const hash = window.location.hash.slice(1) || '/';
const isPartyJoinRoute = hash.startsWith('/party/join/');
const isSharedQuizRoute = hash.startsWith('/quiz/play/');

// Redirect to welcome if needed (unless using shared link)
const showWelcome = await shouldShowWelcome();
if (showWelcome && !isPartyJoinRoute && !isSharedQuizRoute) {
  window.location.hash = '#/welcome';
}
```

### Why `/quiz/play/`

- `/quiz/play/ID` is the entry point for shared quiz links - new users arrive here
- Other quiz routes (`/quiz/create`, `/quiz/results`) are internal navigation
- Consistent with the party join approach from #128

---

## Acceptance Criteria

- [ ] New users clicking shared quiz links go directly to the quiz
- [ ] Quiz loads correctly from the shared ID
- [ ] Existing users (who completed onboarding) are unaffected
- [ ] Direct navigation to `/welcome` still works for new users
- [ ] Party join links continue to work (regression check for #128)

---

## Files to Change

| File | Change |
|------|--------|
| `src/main.js` | Add shared quiz route check to onboarding bypass logic |

---

## Testing Plan

### Automated Testing (E2E)

Create E2E test file: `tests/e2e/shared-quiz-onboarding.spec.js`

Tests to cover:
1. New user with shared quiz link bypasses onboarding and sees quiz
2. New user accessing app directly (no shared link) still sees onboarding
3. Existing user with shared quiz link goes directly to quiz
4. Party join links still work (regression test for #128)

### Manual Testing Checklist

- [ ] New user + shared quiz link → lands on quiz screen
- [ ] New user + direct app access → sees onboarding as normal
- [ ] Existing user + shared quiz link → lands on quiz screen
- [ ] Party join link still works for new users

### Verification Steps

```bash
# Run E2E tests
npm run test:e2e

# Manual: Open incognito/private window
# Navigate to: https://saberloop.com/app/#/quiz/play/TESTID
# Verify: Quiz screen appears (not Welcome screen)
```

---

## Pre-Implementation Checklist

- [x] Root cause identified
- [x] Solution designed
- [x] Testing plan defined
- [ ] Branch created
- [ ] Plan validated by user

---

## Screenshots

### Before Fix (Current Behavior)

<!--
Capture: New user clicks shared quiz link → redirected to onboarding
Save as: docs/issues/143-before.png
-->

_Screenshot pending - will capture during implementation_

### After Fix (Expected Behavior)

<!--
Capture: New user clicks shared quiz link → quiz loads directly
Save as: docs/issues/143-after.png
-->

_Screenshot pending - will capture after fix_

---

## Branch & Commit Strategy

### Branch

```
fix/issue-143-shared-quiz-onboarding
```

### Commit Plan

| Order | Commit Message | Status |
|-------|----------------|--------|
| 1 | `test(e2e): add failing test for shared quiz onboarding bypass` | Pending |
| 2 | `fix(onboarding): allow shared quiz routes to bypass welcome redirect` | Pending |
| 3 | `docs(issues): mark issue 143 as fixed` | Pending |

---

## Implementation Notes

_To be filled during implementation_

---

## References

- Related issue: #128 (party join onboarding bypass)
- Router handling: `src/core/router.js`
- Onboarding redirect: `src/main.js`
- Onboarding logic: `src/features/onboarding.js`
- QuizView: `src/views/QuizView.js`
